apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: dependency-track-api-server
spec:
  hostnames:
    - dependency-track-api-server.localhost
  parentRefs:
    - kind: Gateway
      name: traefik-gateway
  rules:
    - backendRefs:
        - namespace: dependency-track
          name: dependency-track-api-server
          port: 8080
